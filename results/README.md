# 本地实验输出

训练权重、逐spot预测、完整训练日志和临时审计在这里生成，不提交Git。
每次训练使用独立目录，保存 `protocol.json`、`history.json`、`best.pt`、
`selection.json` 和 `validation.npz`。

可随Git发布的小型结果快照位于 `benchmarks/liver/`；
阅读结论与限制请看 `docs/RESULTS.md`。
