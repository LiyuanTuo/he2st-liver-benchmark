# 实验输出

此目录保存本地训练权重、预测矩阵和日志，不随 Git 分发。

每个训练目录包含 `protocol.json`、`history.json`、`best.pt`、`selection.json` 和 `validation.npz`。不同配置使用独立目录。

提交到仓库的结果位于 [benchmarks/liver](../benchmarks/liver)，主要比较见[集成结果](../docs/ENSEMBLE.md)。
