# 历史实验与论文复现

这里保留编号脚本作为实验记录，避免移动后破坏旧结果的复现路径。
**新实验统一使用根目录 `run.py`、`configs/` 和 `src/he2st/`，无需按编号依次运行。**

| 工作流 | 主要脚本 |
|---|---|
| 下载与原始数据检查 | `00_*`、`02_*`、`05_*` |
| 固定训练集高表达面板 | `34_build_heg_panel.py` |
| 七种原基线的HEG实验 | `35_*`–`43_*`、`29_train_stnet_densenet.py` |
| ResSAT原论文核验及指标诊断 | `44_*`、`50_*`、`50b_*`、`50c_*`、`51_*`、`52_*` |
| 三尺度ContextFusion与计数诊断 | `57_*`–`65_*`、`67_*`、`69_*`、`70_*` |

临时调试脚本、PPT 更新工具和旧流水线包装脚本已移除，历史版本可通过 Git 查询；训练、推理和被其他脚本导入的依赖均保留。

旧脚本可能依赖本地 `third_party/` 或特定历史缓存，
它们不是核心包的依赖。原始论文来源见根目录 `THIRD_PARTY.md`。
准备数据及当前训练方式见 `docs/QUICKSTART.md`。
